• No mutation if permission not granted
• Avoid multiple returns
• Use constants prorerly
• Use curly braces everywhere needed
• Always work within given leetcode constraints
• No recursion at all
• Document every part of code and idea itself
• Use block scope
• Use proper variable & function names, compiler will optimize it later
